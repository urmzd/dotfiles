# Direnv configuration — global behavior

[global]
# Suppress loading/unloading messages for cleaner shell startup
silent = true

# Keep a reasonable timeout for environment loading
warn_timeout = "5s"

# Disable stdin to prevent interactive prompts
disable_stdin = true
